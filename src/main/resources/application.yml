spring:
  application:
    name: dbquerywatch-sample
  datasource:
    url: jdbc:postgresql://localhost:5432/test?loggerLevel=OFF
    username: test
    password: test
  flyway:
    placeholders:
      autoIncrement: 'INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY'
  jackson:
    property-naming-strategy: SNAKE_CASE
  jpa:
      generate-ddl: false
